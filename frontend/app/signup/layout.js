export const metadata = {
	title: "Watch.io - rejestracja",
	description: "Zarejestruj się na platformę",
};

export default function SignupLayout({ children }) {
	return <section>{children}</section>;
}
